总体思路：对于测试用例的规约缩减，总体思想是根据函数循环中的判断条件和循环体的内容来分别对测试用例进行规约操作。若reduce函数是根据输入的测试用例在整个测试用例中的位置来进行操作的，则对选择的位置进行排列组合，而未被选择的位置上的测试用例可以不变。若reduce函数是根据输入的测试用例的值进行操作的，则将含有所选择的值的所有测试用例进行排列组合，其他非所选择值的位置可以不变。

具体思路：
1、	建立一个列表list存储测试用例以及测试用例衍生出的变量名称。
a)	最开始将函数的输入变量（最初的存储测试用例的变量）放入list中
b)	根据函数依次判断赋值符号“=”中的右部是否含有list中的变量，若存在，则将赋值号的左部放入list中。
2、	在函数中寻找带有if等判断条件的循环结构。
a)	若在if的判断条件中或者和判断条件相关的变量中有测试用例相关的变量（在list中），则说明此判断与测试用例的值相关。
b)	若在if的判断条件中或者和判断条件相关的变量中没有测试用例相关的变量（不在list中），则说明此判断与测试用例的位置相关。
3、	测试用例的规约：
a)	若reduce函数是根据输入的测试用例在整个测试用例中的位置来进行操作的，则对选择的位置进行排列组合，而未被选择的位置上的测试用例可以不变。
b)	若reduce函数是根据输入的测试用例的值进行操作的，则将含有所选择的值的所有测试用例进行排列组合，其他非所选择值的位置可以不变。
4、	其他：
a)	在进行测试用例规约时，需要加一条将测试用例反向调换的测试用例，此测试用例可以用来排除对所有数据进行累加等类似二元计算的不满足可交换性的特殊情况，例如int的加法满足可交换性，而String的加法得到的结果不可交换。
b)	对于函数中的循环体中没有判断条件的函数，暂时想法是将n条测试数据进行第一条放到最后一条，剩下的每个都向前移一位的方式进行进行规约，因为此方式对于任意两条数据都会改变其相对位置，能测出绝大部分的不可交换性。

实验思路：
1、	首先从网上寻找reduce函数进行采集
2、	对采集的reduce函数进行处理，将mapreduce的特有的数据结构通过java的基本数据结构进行替换。Reduce函数的运算逻辑等不变。
3、	根据不同reduce函数的输入格式要求随机生成测试用例。
4、	対生成的测试用例进行排列组合，生成n！个测试用例，用来判断reduce函数是否可交换。
5、	对reduce函数进行使用提出的方法进行分析，并规约测试用例。
6、	对规约后的测试用例进行测试，验证是否能测出函数的不可交换性。


与位置有关的操作：在for前面定义一个变量并初始化为零，并在for循环中进行每次循环加一，最后在if判断中输出此变量。
与值有关的操作：在if判断中直接输出。
